server:  
  port: ${PORT:8001}

logging:
  level:
   org.springframework:  debug

spring:
  application:
    name: service1

eureka:
 client:
   serviceUrl:  
     defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
     
management:
 endpoints:
  web: 
   exposure:
    include: hystrix.stream
#  base-path: /